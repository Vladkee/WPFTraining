<Window x:Class="_2019._01._25CustomerMVVM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_2019._01._25CustomerMVVM"
        xmlns:view="clr-namespace:_2019._01._25CustomerMVVM.View"
        xmlns:viewModel="clr-namespace:_2019._01._25CustomerMVVM.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="250" Width="800">
    <Window.DataContext>
        <viewModel:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <view:CustomerView Grid.Column="1" DataContext="{Binding Path=SelectedCustomer}"/>
        <ListView ItemsSource="{Binding Path=Customers}" SelectedItem="{Binding Path=SelectedCustomer}"/>
    </Grid>
</Window>
